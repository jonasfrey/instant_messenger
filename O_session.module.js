class O_session{
    constructor(
        n_id, 
        n_o_user_n_id, 
        s_session_token
    ){
        this.n_id = n_id
        this.n_o_user_n_id = n_o_user_n_id
        this.s_session_token = s_session_token
    }
}